name: Printing the output 
on: 
  workflow_dispatch:
      inputs:
        name:
      description: 'person to greet '

      default: 'kanishka'
          
      required: true

      jobs:
      greet:

      runs-on: ubuntu-latest
      steps:
           - name: send greeting
             run: echo "Hello ${{github.events.input.name}}"

            

